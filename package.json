{
  "name": "emerald-wallet",
  "private": true,
  "scripts": {
    "build": "CI=true lerna run build --stream",
    "test": "CI=true lerna run test --stream"
  },
  "devDependencies": {
    "lerna": "^3.13.0"
  }
}

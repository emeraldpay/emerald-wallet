{
  "name": "emerald-wallet",
  "private": true,
  "scripts": {
    "clean:node_modules": "rimraf node_modules packages/*/node_modules",
    "dev": "lerna run build:all:nowatch && lerna run start:electron",
    "build": "lerna run build:all:dev",
    "start": "lerna run start:electron",
    "test": "lerna run test --stream",
    "lint": "lerna run lint --stream",
    "build:dist": "lerna run build:dist --stream",
    "test:coverage": "lerna run test:coverage --stream"
  },
  "devDependencies": {
    "lerna": "^3.13.0"
  },
  "workspaces": [
    "packages/*"
  ]
}

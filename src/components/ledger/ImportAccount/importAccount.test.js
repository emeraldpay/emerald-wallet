import React from 'react';
import { shallow } from 'enzyme';
// import { ImportAccount } from './importAccount';

test('Ledger not connected', () => {
  // TODO: fix it
  // const component = shallow(<ImportAccount connected={ false } />);
});
